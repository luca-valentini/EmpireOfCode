#String Length

Define a function that computes the length of a given string.

###Input: String.

###Output: It's length.

###Example:
```javascript
str_length("") == 0
str_length("mo") == 2
str_length("length") == 6
```
